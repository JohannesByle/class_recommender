import { createMuiTheme } from '@material-ui/core/styles';

var theme = createMuiTheme({
    palette: {
        primary: {
            main: "#000000"
        },
        secondary: {
            main: '#f44336'
        }
    }
});
export default theme;